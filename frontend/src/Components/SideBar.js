import { Box } from '@mui/material'
import React from 'react'

function SideBar() {
  return (
    <Box sx={{backgroundColor:"yellow"}} flex={1} p={2}>
        Left
    </Box>
  )
}

export default SideBar
